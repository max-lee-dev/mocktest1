/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['i.pravatar.cc', 'picsum.photos', 'firebasestorage.googleapis.com', 'localhost'],
  },
}

module.exports = nextConfig 